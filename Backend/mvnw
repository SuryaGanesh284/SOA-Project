#!/bin/sh
DIR="$(cd "$(dirname "$0")" && pwd)"
MVN_CMD="$DIR/.mvn/maven/apache-maven-3.9.9/bin/mvn"
if [ ! -f "$MVN_CMD" ]; then
    echo "Maven binary not found at $MVN_CMD"
    exit 1
fi
exec "$MVN_CMD" "$@"
